// scriptcontributors.js

document.addEventListener('DOMContentLoaded', () => {
    console.log('scriptcontributors.js berhasil dimuat untuk halaman kontributor!');
    // Tambahkan kode JavaScript khusus halaman kontributor di sini jika diperlukan.
    // Contoh:
    // const contributorCards = document.querySelectorAll('.contributor-card');
    // contributorCards.forEach(card => {
    //     card.addEventListener('mouseover', () => {
    //         card.style.transform = 'scale(1.03)';
    //         card.style.transition = 'transform 0.3s ease-in-out';
    //     });
    //     card.addEventListener('mouseout', () => {
    //         card.style.transform = 'scale(1)';
    //     });
    // });
});
